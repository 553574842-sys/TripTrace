apiVersion: v1
kind: Service
metadata:
  name: {{ include "triptrace.fullname" . }}
  labels:
    app: {{ include "triptrace.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "triptrace.name" . }}
